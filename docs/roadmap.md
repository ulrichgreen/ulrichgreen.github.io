# Roadmap

Planned work, in rough priority order.

## Inline Critical CSS

Inline above-the-fold styles in `<head>`, async-load the rest. Worth doing once the design settles.

## Content Audit Follow-ups

Keep `pnpm run audit-content` read-only. If the archive grows enough for the report to become noisy, add focused filters before adding thresholds or write operations.
